package main.java.com.demoexcalculator.demo_ex_calculator.model;

public enum Operation {
  ADD,SUB,MUL,DIV,

  public static Operation of(String operation){
for(Operation operation:Operation.value()){
 if(o.name())
}
  }
}
